(* Programa que recebe um número inteiro positivo e fornece o valor do fatorial desse número usando o comendo for to.  *)

program Fatorial (input, output);
var
   Num, Fat, I : integer;
begin
   read (Num);
   Fat := ;
   for I :=  to Num do Fat := Fat * I;
   writeln ('Fatorial = ', Fat)
end.
